import classes from "./CreateGame.module.css";
const CreateGame = () => {
    return (
        <div className={classes.CreateGame}>
            Create Game
        </div>
    );
}

export default CreateGame